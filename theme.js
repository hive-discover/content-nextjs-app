import { createTheme } from "@mui/material/styles";

export default createTheme({
    palette: {
        mode : "dark",
        type : "dark",
        primary: {
          main: '#9400a4',
        },
        secondary: {
          main: '#d03830',
        },
        divider: 'rgba(197,197,197,0.12)',
      },
});